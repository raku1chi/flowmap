# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 現状

flowmap は「SPA を自動探索して画面遷移をフローチャートとスクリーンショットで一望する」ツール。
設計の情報源は `DESIGN.md`、実行手順は `README.md`。本ファイルはその要点と、複数章を読まないと分からない不変条件だけを書く。

- 実装は `src/`。TypeScript を tsx で直接実行し、ビルド工程はない（tsx は実行時の依存）。Node.js 22 以上。
- `demo/` はビルド不要のデモ SPA（Node の http サーバー + vanilla JS）。flowmap の設計要素と、実アプリで探索を惑わせた仕掛け（localStorage に残る表示設定、入力で開く検索候補、ページ送り、語のデータ区間、target=_blank、行ごとの「〇〇を比較に追加」と sessionStorage に残る比較トレイ）を持つ。
- テストは `test/unit`（ブラウザ不要）と `test/browser`（Chromium を使う。デモを実際に探索する E2E を含む）。挙動を変えたら E2E の約束（重複しない・その場の変化を別の画面にしない・並列数によらず同じ・押してはいけない操作を押さない・差分とゲート）が崩れていないかを必ず確かめる。
- Jev（typesafe.ai の型付き判定モデル）は任意・実験的な補助で、既定は無効。有効にしても既定では危険な操作の判定だけを行い、画面の同定（合流・データ区間）には使わない（判断の根拠は DESIGN.md §5）。問いと閾値の試験は `experiments/jev/`。API キーは `.env`（`.env.example` をコピー）に置き、git に入れない。

## コマンド

```sh
pnpm install && pnpm exec playwright install chromium   # 初回のみ
pnpm typecheck                 # tsc --noEmit（src・test・experiments）
pnpm test                      # 単体テスト
pnpm test:browser              # ブラウザを使うテスト（デモを空きポートで起動して探索する。4 分ほど）
pnpm check                     # 上の 3 つ
pnpm demo                      # デモアプリを http://localhost:3210 で起動（DEMO_BREAK=1 で商品 API を壊す）
pnpm explore                   # flowmap.config.json を読んで探索 → flowmap-out/runs/<ISO日時>/ に出力し index.html・summary.md も生成
pnpm explore --url <URL> --storage auth.json --max-states 30 --workers 4 --baseline <dir> --fail-on new-errors,removed
pnpm render flowmap-out/runs/<dir>   # graph.json からビューアだけ再生成
```

CLI 面は `explore` と `render` の 2 つだけで、これ以外のサブコマンドは増やさない方針（§3）。機能はフラグで足す。
リポジトリ直下の `flowmap.config.json` はデモ用（起点 localhost:3210）。
同梱と違う Chromium を使うときは環境変数 `FLOWMAP_CHROMIUM_PATH` に実行ファイルを指定する（テストも同じ変数を読む）。`FLOWMAP_DEBUG=1` で各画面の骨格（debug-structures.json）と処理ごとの所要時間を出す。

### 実装上の注意

- `page.evaluate` と `addInitScript` に渡す関数（`src/inpage.ts`）は外側スコープを参照しない自己完結の関数として書く。tsx(esbuild) が挿入する `__name` ヘルパーはブラウザに無いので、最初の init script（`NAME_SHIM`）で恒等関数を定義している。`NAME_SHIM` 自体には名前付きの関数を書かないこと。
- ビューアの HTML/CSS/JS は `render.ts` 内の文字列。graph.json は `<` を `<` にエスケープして `<script type="application/json">` に埋め込む。ビューアのスクリプトは `String.raw` なので `${` を書かないこと。
- `render.ts` 内に U+2028/2029 のリテラルを書くと esbuild が正規表現の終端と誤認する。`String.fromCharCode` で扱うこと。
- ビューアのレイアウトは「各ノードに最初に到達した辺（発見辺）」を木とみなす tidy tree。探索は深さごとに全試行を取り込んでから次の深さに進むので、発見辺の from は必ず depth-1 のノードになり、列 = 深さと矛盾しない。探索順や合流（学習時の合流は同じ深さどうしに限る）を変えるときはこの前提を崩さないこと。
- ビューアの着地は画面数で決まる（`LANDING_MAX_TREE` = 40 以下ならサムネイルなしの全画面の木、超えたらグループ図）。木はサムネイルなしが既定で、これは「構造は木で読み、絵は右パネルで見る」という役割分担による。
- ビューアの読む単位は「グループ」と「操作フロー」で、全画面の木は索引。グループ = 探索の木で起点の子ごとの部分木（起点と同じ URL の状態は起点のグループ）。グループをまたぐ辺のうち共通ナビは関係図にも札にも出さない。ここを崩すとグループの関係図が完全グラフになる。コード上の識別子は chapter のまま。
- ビューアは「ほぼ全画面から同じ行き先へ向かう辺」を共通ナビとして畳む（既定で非表示、トグルで表示）。探索が押さずに推定した辺（`inferred`）もここに入る。各ノードに最初に到達した発見辺と失敗した辺は畳まない。この不変条件を崩すと到達経路の見えないノードができる。
- 地図に描くのは別の画面へ移る操作だけ。画面の中で完結する操作（`localActions`）は右パネルに同じ形ごと 1 行で出し、自画面への辺は描かない。その場の変化を経る辺（`via`）は「[〇〇を比較に追加] → 比較ページで開く」の 1 本で描き、クリック数に途中の操作も数える。
- ビューアの画面名は、タイトルのデータの部分（URL のデータ区間・データの見出し・実例どうしで違う部分）を「〇〇」に置き換えたテンプレート名（§8）。同じ辞書（`KNOWN_NAMES`。行ごとの同種の操作の `pattern` から取った名前を含む）で、辺のラベル・途中の操作・フロー名・この画面の中の操作・副題の企業名や商品名も〇〇にする。失敗した操作は元のラベルのまま。表示だけの処理で、シグネチャには関わらない。
- 設定のキーを足すときは `src/types.ts`（型）・`src/config.ts`（既定値と `SPEC` の検証）・DESIGN.md §11 を揃える。`SPEC` に無いキーは「知らない設定」としてエラーになる。

## アーキテクチャ（§3）

探索エンジン・グラフモデル・ビューアの 3 層を **graph.json で切る**。レンダラは graph.json だけから index.html を描けるので、探索をやり直さずに見た目だけ直せる。

| モジュール | ファイル | 役割 |
| --- | --- | --- |
| 型 | src/types.ts | 設定と graph.json の形。`SCHEMA_VERSION`・`SIGNATURE_VERSION` |
| 設定 | src/config.ts | 既定値、コメント可の JSON の読み込み、検証（知らないキー・型・正規表現）、CLI 引数による上書き |
| 正規化 | src/normalize.ts | URL のデータ区間（ID・宣言・自動学習）、クエリの扱い、骨格とシグネチャ、同じ形の操作の畳み込み。ブラウザに依存しない純粋関数 |
| ブラウザ内 | src/inpage.ts | 操作対象の列挙（shadow DOM 含む）、範囲を絞った自動入力、DOM の変化の記録 |
| ワーカー | src/session.ts | コンテキストの作り直し、落ち着き待ち、操作の再特定とクリック、撮影、エラー収集 |
| 探索エンジン | src/explore.ts | 深さごとの幅優先探索、決まった順番での取り込み、共通の操作の推定、学習と再キー付け、graph.json の組み立て |
| 差分 | src/diff.ts | 比較対象の選択（`--baseline`、途中で止まった実行は選ばない）と差分計算 |
| CI 向け出力 | src/report.ts | ゲート（`--fail-on`）の判定、summary.md / summary.json |
| マスク | src/mask.ts | 生成物に残す URL のトークン等を伏せる |
| Jev 判定（任意） | src/jev.ts | 問いの定義、画面の要約、API 呼び出し、答えのキャッシュ |
| レンダラ | src/render.ts | graph.json → 単体で開ける index.html |
| CLI | src/cli.ts | explore / render。終了コード 0/1/2/3/130 |

出力は 1 実行 1 ディレクトリ: `flowmap-out/runs/<ISO日時>/{graph.json, shots/sNNN.png, index.html, summary.md, summary.json}`。
差分は既定で同じ outDir 内の直前の（最後まで探索した）実行と比較し、`--baseline` で明示できる。graph.json のスキーマ（meta / nodes / edges / diff）は §9。

## 変更時に守る不変条件

- **画面の同一性はシグネチャで決まる**（§5）。`sha1(normalize(path) ‖ structure)`。ID に見える区間（数字・UUID・数字と英字の混じった 8 文字以上）とデータ区間は `*`、それ以外の数字は `#`。クエリは既定で見ない（`structuralParams` は値を、`dataParams` は `*` を残す）。骨格はダイアログ・選択中のタブ・見出し（重複は 1 行）・操作対象・フォーム項目で、同じ形の操作は 1 つに畳み、同じルートへのリンクと開閉のラベルと volatile の中は入れない。画面の中のデータ（データのルートへのリンクのラベル、共通の前置き・後置きを持つボタンの群。`pageData`）は見出しとラベルから消す。データ区間を持つルートと、クエリで絞り込む一覧と学習したルート（`queryVariantPaths`）では h1 の文言を見ない。ここを緩めるとノードが無限に増え、厳しくすると別画面が混ざる。設計の要なので変更は慎重に。**計算方法を変えたら `SIGNATURE_VERSION` を上げる**（前回比に注意書きが付き、ゲートで消失を数えなくなる）。
- **シグネチャは決定的に保つ。** 外部の判定（Jev など）や時刻・乱数・並列のタイミングに依存させない。Jev の合流（`jev.pages`）とデータ区間の確認（`jev.dataSegments`）は既定で無効のままにする。
- **ノード id（s001…）は実行ごとに振り直される。実行間の比較は必ずシグネチャで行い、id を突き合わせてはいけない**（§9）。id は取り込み順で決まり、学習で合流させたノードの番号は欠番になる。
- **探索の正しさは「起点から経路を再現する」ことで担保する**（§4）。再現は毎回 storageState だけを持った新しいブラウザコンテキストで行い、前の試行の localStorage・Cookie・メモリの状態を持ち越さない（実アプリで同じ画面が 6 重に分かれた原因）。ブラウザの「戻る」は、リンクで遷移しストレージが変わらなかったときの近道としてのみ使い、戻った先のシグネチャとストレージが元と一致したときだけ再現を省く。一致確認なしに「戻る」の結果を信用するコードを書かないこと。
- **結果は並列数やタイミングによらず同じにする**（§4）。ワーカーは試行して結果を返すだけで、ノードの作成・辺・学習・共通の操作の判定は `mergeOutcome` で「画面の順 → 操作の順」の決まった順番に行う。タイミングで結果が変わる判断をワーカー側に入れないこと。E2E テストが並列 4 と 2 の結果の一致を確かめている。
- **共通の操作の推定・省略は「きれいな」画面でだけ行う**（§4）。起点からリンク・タブ・開閉・検索だけで来て、ストレージが変わっていない画面のこと。ボタンや送信を経た画面ではカートの中身などが違いうるので、共通の操作も押す。回数は別々の画面の数で数える。
- **自動入力は押す送信のフォームだけ**（§4）。画面の全フォームを埋めると、押していない検索欄に候補が開いて画面が変わり、他の操作を覆う。
- 「変化」= シグネチャ同一で textHash が違う画面。データ差でも点灯する参考情報なので **CI のゲート条件に入れない**（§10）。ゲートは `--fail-on` の new-errors・removed・errors・failed-actions で、外部サイトは数えない。比較の前提が違う（`diff.unreliable`）ときは removed を数えない。
- 骨格が変わった画面は「消失＋追加」の組で出る。これは誤検知ではなく仕様（§10）。
- モーダルとタブの切り替えは意図的に別ノードにする（§5）。それ以外の**画面の中で完結する変化**（同じルートで、ダイアログとタブが同じ、見出しを失わず、操作の 8 割以上が残る。開閉・比較パネルへの追加・並べ替え）は別ノードにせず、元の画面の `localActions` に吸収する（§4、`absorbLocalChanges`）。変化で現れた操作は `via` を持つ続けての操作として同じ深さの最後に試す（2 段まで）。吸収した状態のシグネチャは元のノードに結び付けるが、「戻る」やその場に留まる近道は代表と同じシグネチャのときだけ使う（`isAt`。吸収した状態から次の操作を続けない）。
- ストレージに残った部品（比較パネル）が他の画面に出た状態は、部品の行（吸収した変化で現れた行）だけが増えたなら既存の画面に合流させる（`variantOf`）。部品として見たことのない行が増えたら別の画面のまま（カートの「購入手続きへ」を見落とさないため）。
- 外部サイトは既定で押さない（`followExternalLinks: false`）。画面ごとに `externalLinks` を残し、差分では外部サイトを消失に数えない。
- **認証は storageState のみ**。ツールにログイン手順を持たせず、ID・パスワードを `flowmap.config.json` に書かない。storageState ファイルは `.gitignore` に入れる（§6）。
- **探索は本物のクリックと送信を行う。本番環境には向けない。** 安全層は `denyText` / `denySelectors`（`[data-flowmap-ignore]`）/ `denyUrlPatterns`、`allowSubmit`、`allowedHosts`（外なら `--yes` 必須）、別オリジンは撮影のみで操作しない、画面数・深さ・操作数・時間・タイムアウトの上限（§7）。除外文言の既定を削らないこと（単体テストが確かめている）。
- ビューアは**外部ライブラリなしの単体 HTML**で、graph.json を埋め込んで生成する。ファイルを開くだけで動くことが要件（§8）。
- 生成物（index.html / graph.json / summary）に認証情報を含めない（§6）。URL は `maskUrlParams` に当たる値を伏せてから書き出す（`Explorer.buildGraph`）。
- **Jev を有効にすると画面の要約を外部 API に送る**（§7）。送るのはタイトル・見出し・ラベル・フォーム項目・URL のパスとクエリ（機微な名前の値は伏せる）だけで、認証情報・スクリーンショット・本文は送らない。既定を有効に変えないこと。

## 参考

- 拡張候補の優先順位（ピクセル比較 → 入力パターン → 設定の提案 → 経路のテスト化 …）と計画は §12〜§13。
